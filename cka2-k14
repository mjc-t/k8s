# 14. Troubleshooting

**After a cluster migration**, the controlplane **kube-apiserver** is not coming up.

Before migration:

etcd was external and in HA

**After migration**, kube-apiserver is poing to **etcd peer port 2380** instead of **2379**.

fix it.
